def add_integers(i: int, j: int): # Format | parameter: type (type can be int, str, bool, ...)
    return i + j # Editor support added

print(add_integers('ab','cd')) # Doesn't actually restrict anything